# first time git setup
git config --global user.name "Ian Andwati"
git config --global user.email "andwatiian@gmail.com"
git config --global init.defaultBranch main

# :) because why not?
git config --global core.editor nano